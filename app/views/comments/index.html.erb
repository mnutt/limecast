<% @title = "#{h(@podcast.title)} Reviews" -%>
<% @show_ads = true -%>

<h2>All comments for <%=h @podcast.title -%></h2>

<ul class="reviews">
  <%= render :partial => 'comment', :collection => @comments -%>
</ul>
