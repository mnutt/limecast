<div class="episode">
  <% if episode.sources.first.screenshot? %>
  <%= link_to(image_tag(episode.sources.first.screenshot(:square).to_s, :height => 95, :
  <div>
  <% else %>
  <div class="no_screenshot">
  <% end %>
    <h3><%= link_to episode.title, episode_url(:podcast => @podcast, :episode => episode
    <p>
      <span class="details"><%= episode.published_at.to_date -%> - <%= time_to_words(epi
      <%= episode.summary -%>
    </p>
    <p class="links">
      <%= link_to "View 5 min video preview", episode_url(:episode => episode, :podcast 
      <a href="#">Discuss this episode (<%= episode.reviews.count -%>)</a>
    </p>
    <%= render :partial => 'episodes/audio_player', :object => episode if episode.audio_
  </div>
</div>

