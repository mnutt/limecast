<%= link_to_remote "#{current_user.favorite_podcasts.include?(podcast) ? 'Unfavorite' : 'Favorite'}",
    :url => favorite_podcast_url(:podcast => podcast.clean_url),
    :method => :post,
    :html => { :id => "#{dom_id(podcast)}_favorite", 
               :title => "#{current_user.favorite_podcasts.include?(podcast) ? 'This is a favorite.' : 'Favorite this!'}",
               :class => "#{current_user.favorite_podcasts.include?(podcast) ? 'favorite' : 'not_favorite'}" } -%>
