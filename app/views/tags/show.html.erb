<% @title = "Podcasts Tagged '#{@badge || h(@tag.name)}'" -%>
<% @classes = %{podcast index} -%>

<section id="podcasts">
  <h1><%= @badge || h(@tag.name) %></h1>
  
  <br />
  <ul>
    <%= render :partial => 'podcasts/podcast', :collection => @podcasts -%>
  </ul>
</section>

